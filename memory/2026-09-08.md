# 2026-09-08

## 角色更新：主线切换 → 学术研究

Arron 告知律所实习已基本结束（偶有零星法律活），接下来主要用我做学术研究和写论文。讨论了需要调整什么。

**决定：**
- 身份/形象设定（01-IDENTITY、02-SOUL）**不动**——小钳子是跨场景工具定位，不随工作场景改名换性格。
- 改 `03-USER.md`：Arron 画像从"律所实习生、文书代工"改为"法学研究生、学术研究与论文写作为主线、偶有法律活"。
- 改 `04-MEMORY.md`：新增 2026-09-08 主线切换决策；Project Pointers 标注状态——论文（cross-border-employment-thesis）为唯一活跃主线，律所/活动类项目（emergency-management-qa、contract-review、quiz-answer、chengjiu-weilai-vs-ruize、wechat-marketing）标记休眠/随实习结束。

**研究主战场：** 硕士论文写作（劳动法方向，具体选题跨境用工 vs 劳务派遣**待定中**——见 `memory/topics/cross-border-employment-thesis.md`，2027夏开题/2028答辩）。另有其它学术计划待 Arron 后续展开。

**工作方式转向：** 从"法律文书代工"转为"研究搭档"——文献检索 → 材料消化 → 框架 → 论证 → 写作。

## 新工具：DeepSeek 余额查询

起因是 Arron 之前让我装 dsh-whale-widget（查 DeepSeek 余额的小鲸鱼挂件），装好后问能不能在本 agent 里做个类似的。探索后发现 MyAgents 桌面端没有"常驻挂件"宿主，widget 只能渲染在对话里，于是形态定为：**随问随画，余额卡片常驻对话**。

**关键事实：**
- MyAgents `deepseek` Provider 已配且 key 有效（api.deepseek.com，status valid），本会话运行时真打在 DeepSeek 官方。但 MyAgents **产品上读不出 Provider key**（CLI 只有 set-key 无 get-key，凭据库不许读）。
- dsh 的 DEEPSEEK_API_KEY 与 MyAgents 这把是**同一 DeepSeek 账户**（whale balance.json 显示 totalBalance 17.61 CNY）。

**落地（Arron 拍板"随问随画+常驻最新卡片"）：**
- `workspace/.deepseek-key`（gitignore 确认）— key 文件，**Arron 亲自填**，已填好
- `workspace/ds-balance.mjs` — node 脚本，读 key → 调 /user/balance → JSON。无 key exit 2，API 拒绝 exit 1，均测过。
- `workspace/ds-balance-card.md` — 查余额链路约定（含配色语义：≥¥20 主色 / <¥10 warning / <¥3 error）
- 04-MEMORY Technical 新增链路记录。

**✅ 已实测通过（当晚）：** Arron 填好 key 后说"看下余额"，全链路跑通——脚本返回 totalBalance ¥17.24（全部充值、无赠送），widget 余额卡片渲染正常。此后"看下余额"即可随时出卡。

## 威科先行检索 + CARSI 登录态导出

Arron 让我打开威科先行（law.wkinfo.com.cn）查"假外包真派遣"判例。用其**天津财经大学 CARSI**（顶部显示 tjufe_CarSi，走 sso.tjufe.edu.cn 单点）登录成功。

**检索结果（判例素材，本轮交付在对话内）：**
- "假外包真派遣"（主张层表述）→ 20+ 篇，几乎全 2026 新案
- "名为劳务外包实为劳务派遣"（说理层表述）→ 126 篇：高院 9 / 中院 70 / 基层 47
- 精读 4 篇交付：苏州中院(2026)苏05民终10463(穿透→发包方用工单位连带工伤)、重庆高院(2020)渝民申1842(名为外包实为派遣定性的是发包-派遣方间关系,劳动者仍归派遣方)、上海二中(2026)沪02民终4905(劳动者要跟发包方建劳动关系被驳:合意+经济从属性)、长春中院(2026)吉01民终4473(结算模式+管理权归属界定真外包)。规则提炼见对话。
- 用法备忘：威科裁判文书库直链 `law.wkinfo.com.cn/judgment-documents/list?simple=<URL编码关键词>`；结果卡片容器 `.wkb-doc-main-container`；详情页需等加载再取 `.details-mian` innerText。

**登录态导出（Arron 拍板方案 A，三库统一管理）：**
- 文件：`workspace/legal-dbs-login.json`（Playwright storageState，78 cookie + 6 origins ≈ 45KB，workspace/ 已 gitignore）。旧 `wkinfo-carsi-login.json` 已删除并入。
- 覆盖：**威科 + 北大法宝 + 中国知网** 三个库的登录 cookie + 天财 SSO（sso.tjufe/idp）+ 各库 CARSI SP（sp-wkinfo / sp-pkulaw / fsso.cnki.net）。
- **时效**：各库 cookie 数小时~7 天不等（威科 userInfo≈7 天）；天财 SSO 为 session（关浏览器失效，学校 IdP 服务端也在几小时~几天过期）。无"永久登录"，**撑死约 7 天**。
- **恢复**：新会话（浏览器若为新实例）→ 用 playwright-direct 的 `run_code` 调 `page.context().addCookies(json.cookies)` 逐条注入 + 对每个 storageState.origins 重放 localStorage。提示登录/已过期 → 引导 Arron 重新 CARSI 登录后**重新导出覆盖**本文件（删除再 storageState 全量导出）。
- **敏感**：内含天财账号会话凭证，只在本地 gitignored workspace 用，不外传。

**北大法宝 / 中国知网 CARSI 登录（同威科打法，登录顺序建议一次连续做完）：**
- 关键：**先登任一库留下天财 SSO 会话，其余库全部免输号静默通过**（2026-09-08 实测：威科→法宝→知网连续三库，后两个都是 CARSI 一跳即过）。
- **北大法宝**（www.pkulaw.com）：右上"登录/注册"→ 登录 iframe（cas.pkulaw.com，Keycloak）"更多登录方式"→ **校园登录** → ds.carsi.edu.cn 学校发现页 → 搜"天津财经大学"选"登录"。登录后右上角显示法宝账号**手机号 15081808629**。
- **中国知网**（www.cnki.net）：右上"机构登录"→ 面板点 **"校外访问 >"** → 新标签 fsso.cnki.net → 机构搜索框（placeholder"支持汉字、拼音、首字母"）填"天津财经大学"→ 点建议项（href 带 entityID=idp.tjufe.edu.cn）→ 自动跳回。登录后顶部显示"**天津财经大学图书馆**"横幅。检索页为 https://kns.cnki.net/。

## 晚间：cuse 浏览器自动化验证 + 威科已升级 AI+ 界面

Arron 晚上测试我的浏览器/桌面自动化能力（"打开 example.com 读标题""你能自己在威科检索案例吗"），走了真实 Edge + cuse 全链路。发现并验证：

- **`.claude/skills/cuse/` 可用**（Windows CLI `scripts/cuse.exe`）：window list/focus、screenshot --display、click/key/type/scroll、`ui snapshot`（读 Windows UIA 树）。能开 Edge、切标签、点进检索框、输入回车。示例：在 Edge 打开 example.com，UIA 树里读到文档节点 name="Example Domain"。
- **⚠️ 当前 Runtime（deepseek-v4-flash）无视觉**：`Read` 任何 jpg/png 截图都返回 [Unsupported Image]（试过原图/缩放图均失败）。看不了图，只能靠 (a) UIA 树（网页 SPA 内容常不进树，仅外壳）/ (b) OCR 读截图。
- **OCR 读屏工具链已建**：`workspace/ocr-image.mjs`（全文）、`workspace/ocr-words.mjs`（词+bbox 坐标）。要点：tesseract.js v7 须 `recognize(img, {}, { text:true, blocks:true })` 才有 blocks；traineddata 在项目根（chi_sim/eng），配 `gzip:false`；整屏 1430x804 OCR 质量差（误读多、置信度 ~52），小字区可先裁剪放大再识别。点击坐标空间 = 最新截图 JPEG 输出像素（非显示器逻辑像素），OCR 与 click 用同一张图即可对齐。
- **威科先行界面升级**：law.wkinfo.com.cn 现在落到 **威科先行AI+**（URL 带 `/ai/ai-search-home/analysis?conversationId=...`），非早上的经典裁判文书列表。首页有检索AI/合同AI/诉讼AI/问答AI 卡片；"假外包真派遣"→ AI 综合分析模式（左侧 tab：综合分析/相关法规/相关案例/专业解读/官网信息），输出法规条文（如劳动合同法 58 条、劳务派遣暂行规定 27 条）+ 引用文档计数，属语义综述而非判例清单。经典判例直链 `law.wkinfo.com.cn/judgment-documents/list?simple=` 应仍可用（早间已验证）。浏览器内天财 CARSI 登录态在（顶部 tjufe_CarSi）。
- **DeepSeek 余额现值 ¥12.51**（全充值无赠送，环比早间 ¥17.24 降约 ¥4.7）。链路本身无变化。
