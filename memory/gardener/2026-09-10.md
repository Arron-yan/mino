## Gardener Run 2026-09-10

- **lint**: 1 RED / 1 WARN（本机无 Python，按 `memory_lint.py` 的逻辑手动执行全部检查）
  - RED `dead-man`：上次园丁运行 2026-08-16，距今约 600h（阈值 96h）。**查明为机制而非故障**——08-16→09-08 工作区完全静默（git 无 commit、`memory/` 无日志），缺跑区间与静默区间精确重合；MyAgents 定时任务只在应用运行时触发。这条诊断规则已补进 04-MEMORY「低频工作区靠定时任务维持记忆鲜活」。本次运行 `--mark-run` 后归零。
  - WARN `molt`：上次 molt 2026-08-03，距今 38 天（阈值 21 天）。同期模型选型判断已过其自定的衰减窗 → 记入 `memory/gardener/flags-for-molt.md`。
  - 其余全绿：substrate 4/4（01/02/03/04 均在）；budgets 全部达标；log-gap 无（09-08 / 09-09 / 09-10 均 git + 日志双有）；无过期 verified；topic 指针 9/9 全部有效。
- **budgets**: MEMORY 17.6KB / 50KB (35%), USER 1.7KB / 20KB (9%), SOUL 4.1KB / 6KB (69%) — 健康，无需紧急修剪
- **operations**: delete 2, demote 4, merge 2 clusters, add 3
- **drill**: 3/3（见下）
- **flags-for-molt**: 1 条 —— AI 选型结论已过衰减窗且 molt 38 天未跑，需重做模型核验
- **deferred**: 1 条 —— 两个 memory 文件早于本轮即未被跟踪（见 Notes）

### Operations 明细

**delete（2）**
- USER 层「中文沟通，直接不废话。愿意为效率接受新技术工具。」——与 MEMORY「Communication」条目重复，合并保留 MEMORY 那条（并吸收"愿意接受新工具"）。
- USER 层「Arron 的日常核心需求」——仍是实习期口径（合同起草审核/证据整理），与 2026-09-08 主线切换矛盾。改写为学术主线口径，非纯删除。

**demote（4 簇 → 新 topic `memory/topics/legal-research-infra.md`）**
把 04-MEMORY 里三条"写着写着变成操作手册"的技术条目下放：法律数据库访问（威科/法宝/知网 CARSI）、cuse + 无视觉 OCR 读屏、playwright-direct MCP。新 topic 分六节：三库登录与 CARSI、检索入口与取数选择器、浏览器底座、桌面自动化与读屏、机制坑、检索协作方式。04-MEMORY 只保留一条压缩的「法律检索本地底座」+ 强指针（"动手检索前先读"）。
另下放 User Preferences 里的「公众号获客文案写法」——它是项目模板而非稳定偏好，完整模板本就在 `wechat-marketing.md`，USER 层只留触发词 + 一句话 + 指针。

**merge（2 簇）**
- 三条检索基础设施条目 → 一条（见上）。
- 沟通偏好：MEMORY「Communication」与 USER 层重复项去重（见 delete）。

**add（3）**
- Important Decisions 新增 **(2026-09-09) 导师谈话信号：论文疑似转向解释论（效力性/管理性）**。此前 09-09 的进展只落在 topic 与 daily，自动装载层（决策/指针）没有反映——而它直接影响"Arron 一提论文我该准备什么"。措辞严格区分了**已发生事实**（导师说了什么）与**我的推断**（解释论方向，Arron 未确认）。
- Project Pointers 新增 [[legal-research-infra]]、[[gcp-exam]]。
- 更新 [[cross-border-employment-thesis]] 指针：加入 09-09 信号。

**其他修订**
- 「AI 选型结论」删掉过时的"V4-Pro 预计 8 月初上线"，改为标注"本条 2026-08-03 作出，已过衰减窗，待 molt 核验"（不自行改写选型，那是 molt 的层）。
- 「此环境限制」重新核验并把 verified 日期更新为 2026-09-10；Node 版本按实测改为 v24.20.0。
- `memory/topics/cross-border-employment-thesis.md`：删除"选题定位"节里整段重复的 3 行（复制粘贴残留）。

### Drill（3 条抽查）

1. **「低频工作区靠定时任务维持记忆鲜活」(2026-08-03)** — 成立，且本轮拿到新证据：静默期缺跑是机制不是故障。已补证。
2. **「此环境限制」(2026-07-19, verified 2026-08-03)** — 仍成立并重新核验：`python3`/`python` 均不存在（连 skill 自带的 lint 脚本都跑不了）、Node v24.20.0。verified 日期更新。
3. **「公众号获客文案写法」(2026-08-13, USER 层)** — 条目本身有效，但**层级放错**：项目模板不该占自动装载层。已下放到 topic，USER 层留触发词 + 指针。

### Notes

- 工作树基线里已有非本轮改动（多个 skill 的 SKILL.md、`memory/2026-09-08.md`、`memory/topics/quiz-answer.md` 等 modified，以及 `drafts/`、`node_modules/` 等 untracked），全部保留未动；本轮提交只含能明确归属本次记忆维护的文件。
- **deferred**：`memory/topics/gcp-exam.md` 与 `memory/topics/chengjiu-weilai-vs-ruize.md` 在本轮之前就已是 untracked（非本轮产生），故未纳入提交。但 04-MEMORY 的指针已指向它们——意味着这两条指针在别的机器上会悬空。建议 Arron 下次会话（或 memory-update）把这两个 topic 文件一并提交。
- 本轮净减小 753 B。降幅小于"删了三条长条目"的直觉预期，原因是新增的 09-09 决策条与两个指针本身也占体量；MEMORY 仍在预算的 35%，本次的重点是**分层正确**（故事下沉、规则上浮）而非压体积。
