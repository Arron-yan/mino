# 2026-09-10

## Session：知网 → 本机 Zotero 全自动导入链路打通（测试跑）

Arron 要求：在知网找劳务派遣论文，导入本机 Zotero；并提到他在内置浏览器里装了 Zotero Connector 插件。起于功能测试（"我主要是测试一下功能"），一路做到**流程固化成 skill**。收尾时「劳务派遣」分类下 4 篇，均带 PDF 全文（其中 1 篇董保华是 Arron 自己存的）。

### 关键发现

1. **自动化浏览器 = MyAgents 内置浏览器**，Zotero Connector 5.0.212 是 Arron 专门为我装的，带天财 CARSI 知网登录态。⚠️ 我起初从 profile 路径（`AppData\Local\Google\Chrome\User Data\Default`）误判成"Arron 本人的日常浏览器"，被 Arron 纠正。**教训：别把路径巧合当证据，推断用户环境归属前先确认。**

2. **Zotero 真实数据目录是 `D:\Dictionary`**，不是 `C:\Users\ARRON\Zotero`（后者是 2025-12 的旧库）。判定方法：读 Zotero profile 的 `prefs.js` 里 `extensions.zotero.useDataDir` + `dataDir`。程序在 `D:\zotero`。

3. **不需要 Zotero Connector 插件也能导入**：Zotero 自带本机连接器服务 `127.0.0.1:23119`，插件只是它的客户端。直接 POST 同样的协议即可，且能批量。

4. **Zotero 运行时会独占 zotero.sqlite**，直接读报 `database is locked`。解法：拷贝 sqlite + `-wal` + `-shm` 到临时目录再只读打开。

5. **知网全文链接不能交给外部程序抓**（Arron 追问"为什么不能直接让 Zotero 保存"后实测）。`bar.cnki.net/bar/download/order?id=...` 是绑定浏览器会话的一次性订单链接；无 cookie 请求 → `302 /bar/ErrorMsg.html?ErrMsg=来源应用不正确(01)`。Zotero 桌面端没有天财 CARSI 会话，下不了。Zotero 服务端也把这写死了：saveItems 用 `ATTACHMENT_MODE_IGNORE`（注释 "All attachments come from the Connector"）。Connector 插件同样是"浏览器里取字节 → POST 给 Zotero"。→ 我这套只是把字节多过了一次磁盘，机制与插件一致。

### 连接器协议（读 Zotero Connector 5.0.212 + 解包 omni.ja 里 `server_connector.js`/`saveSession.js` 得出）

- `GET /connector/ping` → "Zotero is running"，带 `X-Zotero-Version` / `X-Zotero-Connector-API-Version: 3`
- `POST /connector/saveItems` — 题录。**每个 item 必须自带 8 位客户端 id**（`item.id`），服务端用它做会话内 key 映射（`saveSession.js` 里 `addItem(data.items[index].id, item)`）。payload 的 `target` 字段**被忽略**。
- `POST /connector/saveAttachment?sessionID=...` — 原始字节作 body，元数据走 **X-Metadata 头**（必须 ASCII，title 用英文），字段 `{id,url,contentType,parentItemID,title}`。`parentItemID` = saveItems 时那个客户端 id。**`url` 不能为空**，否则 `importFromNetworkStream` 抛错 → 500。
- `POST /connector/updateSession` — `{sessionID, target, tags, note}`，**必须在 saveItems 之后、用同一 sessionID** 调，把该次保存的条目移到目标分类。`target` 只认**已存在分类的 id**（`"C15"`）或 `"L1"`（我的文库）。
- **建分类做不到**：传分类名 → 500；`createCollection`/`saveCollection`/`addCollection` 等端点全 404；插件中文语言包里根本没有"新建分类"词条（弹窗的「保存到」是**筛选框**不是新建框）。本地 API 也不行——见下节，它是只读的。
- 会话 GC：>10 分钟或 >10 个会话时 1 分钟过期。

### 交付（「劳务派遣」= collectionID 15 / `C15`）

| 论文 | 作者 | 出处 | 附件 |
|---|---|---|---|
| 试论劳务派遣中的同工同酬——兼评2012年《劳动合同法修正案》 (key=BNRBRWLT) | 王全兴; 杨浩楠 | 苏州大学学报(哲社版) 2013, 34(03): 61-69 | 📎 PDF ✅ |
| 劳务派遣的法学思考 (key=TS3WH948) | 董保华 | 2005 | 📎 PDF ✅ ← **Arron 自己存的** |
| 我国劳务派遣的法律规制分析 (key=2ZYU7NNB) | 林嘉; 范围 | 中国人民大学学报 2011, 25(06): 71-80 | 📎 PDF ✅ |
| 劳动派遣关系中的雇主替代责任研究 (key=ZYZQKXTV) | 曹艳春 | 法律科学·西北政法学院学报 2006(03): 116-121 | 📎 PDF ✅ |

- 过程中产生的重复/错误条目（`ZADBHUR5` 无附件重复、`4M9VXK99` 标题存成"自动登录"）**均已由 Arron 手动删入回收站**，分类收尾干净。

### 本地 API 是只读的（我给错了预期，已纠正）

我此前建议 Arron 开本地 REST API 时说"能建分类/删除/去重"——**这是错的**。开完实测：`/api/users/0/...` 的 21 个端点类全是 `supportedMethods = ['GET']`，DELETE 返回 **501**。

- ✅ 能做：实时只读查库（返回 JSON，不用再拷 sqlite 副本）。`GET /collections`、`/collections/<key>/items/top`、`/items/<key>/children`。分类 key：劳务派遣 = `2W93PXB4`。
- ❌ 不能做：建分类、改条目、删条目。

**结论：元数据抓错没有程序化补救，只能手动删。** 所以给入库流程加了自检（标题非空且非"自动登录"、作者不含数字、刊名/页码/PDF 齐备，不过关就不推）。

- 郑尚元《不当劳务派遣及其管制》和谢增毅《美国劳务派遣的法律规制…》也已被 Arron 删进回收站（他要的"2 篇"经几轮调整后变成上面这 4 篇，都是我自己挑的 + 他自存的 1 篇）。

### 提取选择器踩坑（重要）

推林嘉那篇时把标题存成了 **"自动登录"**、作者存成 **"作者发文检索; 林嘉1; 范围2"**。原因：知网详情页里有**多个隐藏的登录浮层 `h1`**，裸 `querySelector('h1')` 命中的是它们，不是文章标题。

正确选择器：
- 标题 → **`.wx-tit h1`**（不要用裸 `h1`）
- 作者 → **`#authorpart a`**（`#author` 那个 li 是"作者发文检索"入口，不是作者区）；作者名里的 `<sup>` 是机构编号，要剥掉，否则变"林嘉1"

**而且改/删都没有程序化入口**（连接器只有创建类端点；本地 API 只读）。抓错只能删了重推 → 后来把校验写进入库流程：不过关就不推。选择器与校验代码最终落在 skill 的 `references/detail-and-push.md`。

### 方案改进：PDF 不再落盘（Arron 追问后重做）

Arron 问"为什么不能直接让 Zotero 保存、非要下载文件"。查清后：

- **不能让 Zotero 自己下** —— 知网全文链接绑浏览器会话。实测无 cookie 请求 → `302 /bar/ErrorMsg.html?ErrMsg=来源应用不正确(01)`。Zotero 服务端也把这写死：`saveItems` 用 `ATTACHMENT_MODE_IGNORE`（注释 "All attachments come from the Connector"）。
- **插件也不是把 URL 交给 Zotero** —— 它在浏览器里取字节再 POST。
- **重做为在扩展 service worker 里跑**（`memory/topics/zotero-cnki-import.md` 有完整说明）：`saveItems` → `Zotero.ItemSaver.saveAttachmentToZotero()` → `updateSession`，全程内存，无磁盘。
- **踩坑**：自己写裸 `fetch(pdfUrl)` 拿回来是 5786B 的 HTML 错误页（CNKI 查 referer）。必须用插件的 `saveAttachmentToZotero`（带 Cookie 头 + referrer），并把知网标签页传给它。

### 流程固化为 Skill

Arron 提出："这种方式能做成 skill 吗？查找什么论文、存哪、存多少、存什么类型我以后可能还有要求，能跨对话复用就好"。

→ 建 `.claude/skills/cnki-to-zotero/`：

```
SKILL.md                      # 参数清单 + 前置检查 + 主流程 + 硬约束 + 交付话术
references/cnki-search.md     # 知网检索与筛选的 DOM 操作（切学术期刊/开来源类别/改被引排序/取结果）
references/detail-and-push.md # 详情页选择器 + 校验 + service worker 推送代码（整段可复制）
references/zotero-protocol.md # 连接器协议全表 + 本地 API + 全部已知坑
scripts/zotero-query.mjs      # 查库验证（本地 API 优先，回收站走 sqlite 兜底）
scripts/zotero-push.mjs       # 备用 Node 版（PDF 落盘中转）
```

参数化设计成：**主题 / 目标分类 / 数量 / 筛选条件（CSSCI、期刊or学位论文、年份）/ 要不要 PDF**，前两项必问。

同时清理：`workspace/zotero-push.mjs`、`workspace/zotero-query.mjs`、`workspace/zotero-import/` 全部删除（内容已进 skill，避免留两份改错）。记忆里的 topic 文件改成指针，不再重复 skill 里的流程细节。

### 其他

- 扒 Zotero 源码（`omni.ja` 解出 `/tmp/zot/.../server_connector.js` + `saveSession.js`）弄清协议细节。
- ⚠️ `updateSession` 内部是 `item.setCollections(collection ? [collection.id] : [])` —— **整体替换**分类归属，不是追加。只影响当次保存的新条目，风险低但要知道。

### 新约定

**Arron 定：以后让我存论文时会直接指定目标分类，不会让我新建分类。**

### 工具（最终都归入 skill，工作区副本已删）

- `.claude/skills/cnki-to-zotero/scripts/zotero-query.mjs` — 只读查库（本地 API 优先，回收站走 sqlite 副本）
- `.claude/skills/cnki-to-zotero/scripts/zotero-push.mjs` — 备用 Node 版（PDF 落盘中转）

## Session：劳务派遣判例双库检索（威科 + 北大法宝，材料轨）

Arron 要求：分别打开威科先行和北大法宝，检索与劳务派遣有关的判决书。产出 `drafts/劳务派遣-判例检索-2026-09-10.md`。

### 命中地图（裁判文书库 / 司法案例库）

| 词 | 威科 | 法宝 |
|---|---:|---:|
| 劳务派遣 | 184,633 | 206,175 |
| 劳务派遣 连带责任 | 37,206 | 42,388 |
| 劳务派遣 同工同酬 | 6,459 | 7,256 |
| 劳务派遣 临时性辅助性替代性 | 8,105 | — |
| 逆向派遣 | 2,185 | 2,359 |
| 假外包真派遣 | 369 | 165 |
| 劳务派遣 典型案例 | 271 | — |
| 劳务派遣 10%比例 | 76 | — |

结论：两库同阶，不是"库里没有"是**词太宽**；可写密度在 逆向派遣 / 假外包真派遣 / 三性 三支。

### 检索语法（本轮新实测，重要）

- **威科**直链 `law.wkinfo.com.cn/judgment-documents/list?simple=<URL编码>`，空格=AND，可用。**首次直连会弹点击式安全验证**（"请依次点击【决,接,忽】"），刷新/二次导航即正常；**无视觉无法破解，也未尝试破解** —— 遇阻时请 Arron 手动点一下。这是实操边界。
- **法宝**快搜框：空格=AND；**仅 `案由:` 字段前缀有效**（`案由:劳务派遣合同纠纷`→27 篇）；`标题:`/`全文:` 前缀无效（静默回落默认全库列表，会误读成"命中 1.7 亿"）。**案号检索极准**：`(2026)苏05民终10463号`→1 篇。
- 抽取：威科结果项 = `b-list-item-judgment-documents`，链接 `a[href*="/detail"]`；法宝 = `a[href*="/pfnl/"]`。
- `browser_run_code` 里 **DOM 操作必须包在 `page.evaluate()`**，直接在 Node 作用域写 `document` 会 `ReferenceError`。

### 最有价值的发现：威科官方典型案例 13 篇

与 09-09 导师信号（虚假派遣 + 效力性/管理性）**直接对上**的 4 篇：
1. 「逆向派遣」行为无效 事实劳动关系依法受保护 —— 肇庆中院典型案例之一（2026.04.30）`MkExMDAwNzY5ODg`
2. 用人单位不得通过逆向派遣规避法律责任 —— 2023 宿迁典型案例之六（2024.10.25）`MkExMDAwNTA1MzI`
3. 用工单位与派遣单位签订的免责协议损害劳动者权益的对劳动者不发生效力 —— 常州中院+人社局 2025 典型案例之三（2026.04.30）`MkExMDAwNzcxOTY` ← **纯效力问题**
4. 未经许可经营劳务派遣业务案 —— 上海市人社局典型案例之六（2025.02.19）`MkExMDAwNTY5NjI` ← 对应 57 条行政许可

另有 盐城「以业务外包之名行劳务派遣之实无效」`MkExMDAwNzcxNjY`、重庆四中/吉林高院连带责任各一篇、珠海工伤两篇、天津河西工龄、北京人社局公告。

### 高院层

逆向派遣支目前**唯一高院**：(2026)晋民申1924号（山西高院·再审审查，2026.07.27）`MjA0MTYyNzg0NDk` —— 优先看。

### 判断

- 裸词 18-20 万不可作素材池。`劳务派遣合同纠纷`案由是**派遣单位 vs 用工单位的商事合同之争**，与劳动者维权混在同一关键词下，分类必须分开。
- `劳务派遣 10%比例` 仅 76 篇且头部是保险合同纠纷 → **10% 比例几乎不进裁判说理**，此点对"效力性/管理性"选题是负信号（无人援引的条款谈不上效力认定）。

### 下一步（待 Arron 拍板，已写进 draft 文末）

逆向派遣支全量索引 / 精读 ⭐4 篇 + 晋民申1924号 / 条文锚定检索（57、66 条、暂行规定 4 条在说理中的援引与定性）/ 法宝按案号逐案回查。

## 补：北大法宝"总是自动登个人号"诊断（Arron 提问后查实）

**Arron 澄清真实诉求**：他**想用天财学校账号**，但法宝老自动登录他的**个人账号**（15081808629），"有些案例我的个人账号可能看不了"。（→ 我先前记忆写成"法宝就是个人号"，方向错了，已改：他要的是学校号，这是故障不是设定。）

**根因**：自动化浏览器 profile 里存着**长驻"记住我"会话**，把机构登录挡在门外。
- `cas.pkulaw.com`：`KEYCLOAK_REMEMBER_ME` → **2027-09-10**；`KEYCLOAK_SESSION` → 2026-10-10
- `.pkulaw.com`：`loginType` / `preferred_username` / `SUB` / `session_state`
- **profile 内无任何 `tjufe.edu.cn` / `carsi.edu.cn` cookie** → **CARSI 那条腿从未跑通过**
- 机制：会话自动恢复 → 法宝从不弹登录框 → 校园登录没机会执行。**不是账号绑定问题，是长驻会话遮蔽。**

**修法**（属"重新登录"类，未动手，已交 Arron 拍板）：备份现状态 → 退出法宝 → 重走校园登录（需 Arron 本人输天财账号密码）→ 不勾"记住我" → 导出覆盖 `legal-dbs-login.json`。

**读屏手法沉淀**：诊断只看 cookie 的 **name/domain/expires**（不读 value），既能定性又不碰会话凭证。这条以后可复用。

**另**：Arron 同时确认了威科弹验证的边界——他会手动点。约定：**弹出验证我就停下来告诉他，他点完我再继续**。

## 补二：法宝换号 → 判定"换不掉" → 流程固化为 Skill `.claude/skills/legal-db-login/`

**Arron 拍板**：确认"法宝把 CARSI 的天财身份映射到了手机号绑定的个人账号上"，并要求把威科+法宝的登录过程固化成脚本/skill 复用。

### 关键实测结论

1. **法宝退登不彻底**：UI「退出登录」只清应用层，**不动 Keycloak 单点**（`cas.pkulaw.com/KEYCLOAK_REMEMBER_ME`，有效期约一年）。后果是重走校园登录时天财 CAS 页"一闪而过不索要密码"，直接顶回原账号 —— 这就是"总是自动登个人号"的真正机制。**必须按域清 cookie 才能逼出真正的登录表单。**
2. **换号换不掉**：即使完整走通 CARSI（`loginType=carsi` + 天财 IdP/CARSI SAML cookie 齐备），法宝**仍落到手机号账号**，UI 仍标「个人账号」。→ 要独立学校号须找**法宝客服**。
3. **法宝会话最脆**：CARSI 登录后不久，浏览器进程回收 → session 级 cookie 全丢 → **法宝掉线**，而威科、知网（持久 cookie）仍在线。三库抗掉线：威科 🟢 知网 🟢 **法宝 🔴**。
4. **导出正确姿势**：`context.storageState({ path })` —— Playwright 自己写盘，run_code 里没有 `fs` 也能落盘（实测）。
5. **排查归因顺序**：cookie ✅ 是身份来源｜localStorage ❌ 只有统计键（页面有 `#access_token` 隐藏域但没用上）｜IndexedDB 未排查。

### 交付

Skill `.claude/skills/legal-db-login/`：
```
SKILL.md                        # 四入口分诊(重登/导出/身份诊断/恢复) + 前置检查 + 硬约束 + 验证判据
references/login-flows.md       # 三库逐步操作 + CARSI 公共段 + 状态标志（威科路径标注为未完全实测）
references/state-diagnosis.md   # cookie 机制、Keycloak 坑、身份映射、底座故障、无视觉限制、排查清单
references/run-code-snippets.md # CHECK / PURGE_DOMAIN / LOGIN_PKULAW / LOGIN_WKINFO / EXPORT / RESTORE
scripts/inspect-state.mjs       # 离线体检导出文件（域覆盖/过期/session 占比），已实测可用
```

**硬约束写进 skill**：不碰账号密码（Arron 亲输）｜清 cookie 必限域并立即验证另两库未误伤｜遇验证码停手｜改登录态前先备份｜只读 cookie 的 name/domain/expires，不读 value。

### 当前登录态（CHECK 实测）

威科 ✅（`tjufe_CarSi`）｜知网 ✅（天津财经大学图书馆横幅）｜**法宝 ❌ 已掉线**。
`workspace/legal-dbs-login.json` 已用 `storageState({path})` 覆盖为当前状态（法宝那格是掉线状态，**不是好备份**）；09-08 旧导出已备份到 `workspace/legal-dbs-backup-2026-09-10/`。
→ **法宝需要 Arron 重登一次**，登完立刻再导出。

### 教训

- 全程我一度去重新探测威科登录入口，**而流程本来就写在 `memory/topics/legal-research-infra.md` 里** —— 被 Arron 点出"这些登录过程不是在你 memory 里吗"。**写 skill 前先读自己的记忆，别重造。**
- 威科登录入口与 memory 记录不符（登录前导航栏只见"登录"，无 "CARSI 入口"），已在 skill 中如实标注为"未完全实测"并给出待补动作，**没有照猜测写死选择器**。

## 补三：法宝重登成功（用新建的 legal-db-login skill）

Arron 说"重新登一下吧" → 加载 `.claude/skills/legal-db-login/`，走 A. 重登 → `LOGIN_PKULAW`。

### 结果：✅ 三库全绿

| 库 | 页面标志 | 状态 |
|---|---|---|
| 威科先行 | `tjufe_CarSi` | ✅ |
| 北大法宝 | 已登录（无"登录/注册"） | ✅ |
| 中国知网 | 天津财经大学图书馆横幅 | ✅ |

CARSI 段齐备：天财 IdP 2 / 天财 SSO 3 / CARSI SP(法宝) 2。`loginType=carsi`，`preferred_username=wx20250318165626749600`。

### 两个新认知（已回写 skill 与脚本）

1. **神预判被推翻：没要 Arron 输密码。** CHECK 显示天财 cookie = 0，我据此判断"需要输密码"——**错了**。实际重登时 CARSI 全程免密通过（`sso.tjufe.edu.cn` → `idp.tjufe.edu.cn` SAML → `cas.pkulaw.com/post-broker-login`）。**说明天财 SSO 的服务端会话可以比浏览器 cookie 活得久。**
   → skill 里的建议已改成：**别预先假定要输密码，直接试走一遍**；`inspect-state.mjs` 的提示语同步改了。

2. **登录链日志是宝藏。** `page.fill` 超时报错里带着完整导航链，反而把 CARSI 每一步暴露了。以后调试登录流程，**故意让步骤超时反而能看到真实跳转路径**。

### 导出

`workspace/legal-dbs-login.json` 已用 `storageState({path})` 覆盖：**83 cookies / 7 origins**，三库 + CARSI 全含 —— **这是第一份真正完整的登录态导出**（之前两份要么过期、要么缺法宝）。
旧的两份备份留在 `workspace/legal-dbs-backup-2026-09-10/`（09-08 版、重登前版）。

### 体检脚本改进（inspect-state.mjs）

初版每次都报"3 天内到期"——因为把埋点/门槛 cookie（`Hm_*`、`check`、`connect.sid`、`acw_tc`、`userislogincookie`、`LoginAccount`）也算进去了。这些**每次访问自动续期，过期 ≠ 掉线**。
→ 加 `SHORT_TTL` 排除表后，输出变成真信号：威科 09-15 到期、法宝 10-10、知网 10-08，无假警报。
**教训：一个总在报警的警告等于没有警告。**
